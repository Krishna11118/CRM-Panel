const config = {
  // endpoint: `http://localhost:3001/v1`,
  
  // vercel
  endpoint: `https://crm-admin-main-s726.vercel.app/v1`

  //working render
  // endpoint: `https://crm-admin-main.onrender.com/v1`,
};

export default config;
